export const metadata = {
  title: "Configurações",
  description: "Gerencie as configurações principais do aplicativo",
};

import AppSettings from "./AppSettings";

export default function SettingsPage() {
  return <AppSettings />;
}
